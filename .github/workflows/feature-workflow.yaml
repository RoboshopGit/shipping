name: feature-pipeline
on:  workflow_dispatch
  
  #push:
jobs:
  # Job name
  feature-pipeline:
    if: github.ref_name != 'main'
    uses: devops/workflows/.github/workflows/java-feature-pipeline.yaml@main
    with:
      project: 'roboshop'
      component: 'shipping'
    secrets: inherit
    permissions:
      contents: read    # Required to check out code or read the repo
      statuses: write


  